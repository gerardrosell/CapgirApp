<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.prolificinteractive.materialcalendarview.sample">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".BasicActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Event_activity" />
        <receiver android:name=".Button_listener">
            <intent-filter>
                <action android:name="button_click"/>
            </intent-filter>
        </receiver>
        <activity android:name=".Event_List_activity" />
        <activity android:name=".Edit_event_activity" />
        <activity
            android:name=".ContacteActivity"
            android:label="Contactes"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".QuiSomActivity"
            android:label="QuiSom"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RegistreActivity" />
        <activity android:name=".ParticipantsListActivity" />
        <activity android:name=".NoParticipantsListActivity" />
        <activity android:name=".BusParticipantsListActivity" />
        <activity android:name=".Num_acompanyants"></activity>
    </application>

</manifest>